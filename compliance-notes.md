# Accessibility compliance notes

**Project.** BIO 005 Human Physiology, Yuba College, Fall 2026.
Repo `drsrennie-stack/human-physiology-Fa26`.

**Date.** September 7, 2026.
**Reviewer.** Dr. Sharilyn Rennie.

---

## 1. Files covered by this pass

**Competency pages, weeks 1 to 15.** `week-01-competencies.html` through
`week-15-competencies.html`. Weeks 2 to 15 are generated by
`tools/gen_competency_pages.js` from `bio005-competencies.js` and
`assets/bio005-sheet-data.js`, so a fix to the generator applies to all
fourteen. Week 1 is hand built.

**Note sheet.** `note-sheet.html`, all fifteen weeks, at one, two and three
competencies per printed page.

**Shipped PDFs.** The six listed in section 4.

**Also touched, verified for regressions only:** `assignment-physioex.html`,
`door-lab.html`, `course-schedule.html`, `course-start.html`,
`mastery-physio-os-standalone.html`, `worksheet-week02-graphing.html`.

---

## 2. WCAG version and level

**WCAG 2.2 Level AA is the floor and is met on every file above.** Level AAA is
met for contrast on all body text, headings and prompt text. The two places
that sit at AA rather than AAA are named in section 3.

Criteria specifically checked this pass:

| Criterion | Level | Result |
|---|---|---|
| 1.1.1 Non-text content | A | Pass. Every figure carries alt text, on screen and in the PDF structure tree. Decorative SVG is `aria-hidden`. |
| 1.3.1 Info and relationships | A | Pass. `main` landmark, one `h1`, no skipped heading levels, every checkbox tied to a label by `for` and `id`. |
| 1.3.2 Meaningful sequence | A | Pass. DOM order is reading order. Verified in the PDF structure tree, not only on screen. |
| 1.4.1 Use of colour | A | Pass. The five week jump squares each carry a letter, a `title` and a visually hidden name, so colour is never the only signal. A picked prompt changes border and background, not colour alone. |
| 1.4.3 Contrast, minimum | AA | Pass. Measured, section 3. |
| 1.4.6 Contrast, enhanced | AAA | Pass except the two pairs named in section 3. |
| 1.4.11 Non-text contrast | AA | Pass. Card and prompt borders measured against their backgrounds. |
| 2.1.1 Keyboard | A | Pass. Section 5. |
| 2.4.1 Bypass blocks | A | Pass. Skip link to `#main` on every generated page. |
| 2.4.7 Focus visible | AA | Pass. 3px maroon outline, 3px offset, on every interactive element. |
| 2.5.8 Target size, minimum | AA | Pass. Jump squares are 32px, above the 24px minimum. Prompt checkboxes are 22px with the whole label as the hit area, well above minimum. |
| 3.1.1 Language of page | A | Pass. `lang="en"` on every page, `/Lang en-US` in every PDF. |
| 4.1.2 Name, role, value | A | Pass. Progress bar carries `role="progressbar"` with `aria-valuenow`, `aria-valuemin`, `aria-valuemax` and `aria-labelledby`. |
| 2.3.3 Animation from interactions | AAA | Pass. `prefers-reduced-motion: reduce` disables every transition. |

---

## 3. Colour contrast audit

Measured from computed styles in headless Chromium, not read off the palette.

| Text | Foreground | Background | Ratio | Level |
|---|---|---|---|---|
| Competency name `.cname` | #0B1530 | #FFFFFF | 15.65:1 | AAA |
| Competency statement `.can` | #0B1530 | #FFFFFF | 15.65:1 | AAA |
| Prompt text `.prompt label` | #0B1530 | #FFFFFF | 15.65:1 | AAA |
| Prompt letter `.plet` | #6E2D24 | #FFFFFF | 9.24:1 | AAA |
| Progress note `.pnote` | #414B5C | #FFFFFF | 8.80:1 | AAA |
| Group heading eyebrow `.chead .cl` | #8B3A2E | #FAFAF9 | 7.55:1 | AAA |
| Header eyebrow on the dark band | #C9A14A | #060A18 | 9.71:1 | AAA |
| Header h1 on the dark band | #FFFFFF | #060A18 | 19.42:1 | AAA |
| Header sub on the dark band | #D7DCE3 | #060A18 | 13.85:1 | AAA |
| Course home button | #FFFFFF | #0B1530 | 18.04:1 | AAA |
| Jump square N, note sheet | #FFFFFF | #0B1530 | 18.04:1 | AAA |
| Jump square P, problems | #FFFFFF | #8B3A2E | 7.66:1 | AAA |
| Jump square L, lab | #060A18 | #C9A14A | 8.16:1 | AAA |
| Jump square D, discussion | #8B3A2E | #FFFFFF | 7.66:1 | AAA |
| Jump square S, study | #0B1530 | #ECEFF4 | 15.65:1 | AAA |
| **"Pick one and do that one" `.pick`** | **#5A6675** | **#FFFFFF** | **5.07:1** | **AA, not AAA** |
| **Tag pills `.tag`** | **#5A6675** | **#FFFFFF** | **5.07:1** | **AA, not AAA** |

The two AA rows are both small supporting labels that repeat information already
carried in full elsewhere on the card. They clear AA comfortably. Raising them to
AAA is a one-line change in `tools/gen_competency_pages.js` if you want it.

Gold is never used as text or as a border on a light background anywhere in this
pass. It appears as an eyebrow on the near-black band, where it measures 9.71:1,
and as a jump square background carrying near-black text.

---

## 4. PDF accessibility

Every PDF this course shipped before today was Chrome print-to-PDF, which writes
no structure tree. Those files opened correctly and reached a screen reader as
unstructured text: no headings to navigate by, no list or table semantics, no
alt text, and no guaranteed reading order. Five of the seven were untagged.

They are rebuilt with WeasyPrint targeting **PDF/UA-1**, by
`tools/make_pdf.py`. Pages that build themselves in JavaScript are rendered in
headless Chromium first and the settled DOM is handed over, so the PDF matches
what a student sees.

| `BIO005-Fall2026-Competencies-by-Week.pdf` | 31 | yes | yes | en-US | H1 x1, H2 x4, H3 x15 | 0 of 0 |
| `BIO005-Fall2026-Competency-Packet.pdf` | 18 | yes | yes | en-US | H1 x1, H2 x2, H3 x5 | 0 of 0 |
| `BIO005-Fall2026-Syllabus.pdf` | 17 | yes | yes | en-US | H1 x1, H2 x16, H3 x16 | 0 of 0 |
| `BIO005-Week1-Competencies.pdf` | 6 | yes | yes | en-US | H1 x1, H2 x2, H3 x12 | 2 of 2 |
| `BIO005-Week1-NoteSheet.pdf` | 6 | yes | yes | en-US | H1 x1, H2 x12 | 0 of 0 |
| `BIO005-Week2-Graphing-Worksheet.pdf` | 5 | yes | yes | en-US | H1 x1, H2 x4 | 3 of 3 |

Every file above: `/MarkInfo /Marked true`, a `/StructTreeRoot`, `/Lang en-US`,
a title in the document catalog, and `/ViewerPreferences /DisplayDocTitle true`
so a reader announces the title rather than the filename.

Three problems found and fixed during this pass, each named because each would
have shipped silently:

1. **Inline SVG never reached the tag tree.** The three figures on the Week 2
   graphing worksheet carried `role="img"` and a full `aria-label` on screen,
   and WeasyPrint emitted them as bare marked content with no `/Figure` and no
   `/Alt`. A student on a screen reader would have met three undescribed
   pictures with six questions about them. The build now converts each described
   SVG to an `img` carrying the same text in `alt`, which produces `/Figure`
   with `/Alt`. Verified: 3 of 3 figures on the worksheet, 2 of 2 on the Week 1
   competency page.
2. **The note sheet shipped two H1s.** The on-screen page heading was being
   tagged alongside the sheet's own runner. The printed artifact is now scoped
   to the sheet pages only.
3. **The Week 2 worksheet skipped from H1 to H3.** The figure titles are now H2.

The old untagged files are moved to `_superseded/` rather than deleted, and
`syllabus-fall2026.html` now links the tagged build.

---

## 5. Keyboard navigation

Verified on `week-01-competencies.html` and a generated page,
`week-08-competencies.html`, as the largest at 25 competencies.

Tab order: skip link, brand mark, Course home, the five jump squares in order,
then each competency's prompt A and prompt B in document order. Nothing is
reachable that is not operable, and nothing operable is unreachable. No positive
`tabindex` anywhere. No keyboard trap.

Space toggles a prompt checkbox. Ticking B unticks A, which is the intended
one-prompt-per-competency rule, and the change is announced through the
progress bar's `aria-live` region rather than being visual only.

The horizontally scrolling template region on the note sheet carries
`tabindex="0"` and a `role="region"` with an accessible name, so it can be
scrolled from the keyboard.

---

## 6. Screen reader testing

**Automated only, so far.** The accessibility tree was walked in headless
Chromium for all fifteen competency pages and the note sheet, checking landmarks,
heading order, accessible names on every link and control, label association,
and live region wiring. All fifteen pass. The PDF structure trees were audited
directly with pikepdf and are reported in section 4.

**What has NOT been done, and is owed before this is called complete:**

- A pass with **VoiceOver on macOS Safari**, which is what a Mac student most
  likely uses.
- A pass with **NVDA on Windows Firefox**, which is what a Windows student most
  likely uses.
- Opening the six PDFs in **Adobe Acrobat's Accessibility Checker** and in a
  real reader, because a structure tree that audits clean can still read badly.

An automated pass proves the semantics are present. It does not prove the page
is usable, and this document should not be read as claiming it does.

---

## 7. Known limitations and remediation plan

| # | Limitation | Plan |
|---|---|---|
| 1 | No human screen reader testing yet. | VoiceOver and NVDA passes before Week 1 opens. Highest priority item on this list. |
| 2 | `.pick` and `.tag` labels are AA, not AAA. | One-line change in the generator. Waiting on your call, since darkening them changes the visual hierarchy of the card. |
| 3 | Prompt state is per device. A student who ticks on their phone will not see it on a laptop. | Documented on the page in plain language. No fix planned. |
| 4 | `physiology-course-map.html` still carries an older course sequence: six units, endocrine in unit 6, week order that does not match the schedule of record. Exam labels were corrected, the body was not. | Needs your decision: regenerate from `bio005-schedule-fall2026.js`, or retire the page. |
| 5 | Week 3 has no lab worksheet. The osmosis and IV fluids worksheet was unhooked because it is not on the Canvas schedule. Week 6 has no lab either. | Both need a lab named. |
| 6 | "Mass flow and clearance" appears in no week of the competency map. | Needs a home or a deliberate retirement. |
| 7 | The remaining PDFs in `_superseded/` are untagged. | They are out of the student path. Delete once you are satisfied nothing links to them. |

---

## 8. Reviewer

Prepared for Dr. Sharilyn Rennie, September 7, 2026. Not signed off until
section 6's outstanding screen reader passes are done.
